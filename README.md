# 🚀 FERIXDI STUDIO

**Космический хакерский командный центр для ремикса видео**

> Уникализируй видео. Пересобери под себя. Забирай бесплатный трафик.

![License](https://img.shields.io/badge/license-MIT-blue)
![Pages](https://img.shields.io/badge/deploy-GitHub%20Pages-green)

---

## Что это?

FERIXDI Studio — инструмент для создания уникальных коротких видео (8 секунд) с парой персонажей. Studio генерирует полный пакет: photo prompt, video prompt (EN), RU-диалог с таймингами, blueprint и подсказки речи.

**Вайб**: space-hacker premium console — тёмный фон, звёздная пыль, неон, стеклянные панели, scanline, typing эффекты.

## Ключевые возможности

- **121+ персонажей** с уникальными характеристиками, темпом речи, уровнем мата и ролями
- **3 режима ввода**: Идея / Мой диалог / Референс-видео
- **Thread Memory** — импорт/экспорт ветки, компиляция STYLE_MEMORY
- **Character Mixer** — выбор пары A/B, фильтры, бейдж совместимости, swap
- **Video Remix** — загрузка mp4/mov, опции (8s grid, ритм, липсинк)
- **Random Humor** — 10 категорий, случайный выбор
- **Timing Coach** — визуальная шкала 0..8s, оценка длительности, trimming suggestions, авто-сокращение
- **Generate Final Package** — 5 выходов (Photo JSON, Video JSON, RU Package, Blueprint, Log)
- **Валидаторы** — banned words, overlap check, device invisible, location repeat guard
- **History Cache** — предотвращает повторы локаций и реквизита

## Доступ по ключу

Studio защищена UX-гейтом. Ключ хэшируется SHA-256 на клиенте и сравнивается с хэшами в `app/data/access_keys.json`.

**Демо-ключ**: `FERIXDI-DEMO-2026`

Для добавления своих ключей:
1. Сгенерируйте SHA-256 хэш вашего ключа
2. Добавьте в `app/data/access_keys.json`
3. Никогда не храните ключи в открытом виде

## Demo vs API mode

| | Demo | API |
|---|---|---|
| Работает без backend | ✅ | ❌ |
| Нужен API ключ | ❌ | Gemini (опционально) |
| Генерация | Локальная, детерминированная | Через backend |
| Деплой | GitHub Pages | Pages + Render |

## Локальный запуск

```bash
# Установить Node.js >= 18
npm install
npm run dev
# Откроется http://localhost:3000
```

## Деплой на GitHub Pages

1. Push в `main` branch
2. GitHub Actions автоматически:
   - Устанавливает зависимости
   - Прогоняет тесты
   - Собирает Vite build
   - Деплоит на Pages
3. Сайт доступен по: `https://<username>.github.io/ferixdi_studio/`

Убедитесь, что в Settings → Pages → Source выбран **GitHub Actions**.

## Backend на Render

```bash
cd server
npm install
npm start
```

Endpoints:
- `POST /api/auth/validate` — проверка ключа → JWT
- `POST /api/thread/summarize` — компиляция памяти
- `POST /api/fun/category` — случайная категория
- `POST /api/remix/generate` — генерация пакета

Env vars: см. `server/.env.example`

## Тесты

```bash
npm test
```

Unit-тесты для: estimator, validators, auto_trim.

## Структура проекта

```
/app
  /data          — characters.json, access_keys.json
  /engine        — generator, validators, estimator, auto_trim, history_cache
  /spec          — golden_standard.yaml, schemas, ui_copy_ru
  /styles        — Tailwind CSS + кастомные стили
  index.html     — главная страница
  main.js        — логика приложения
/server          — Express backend template
/tests           — unit тесты (vitest)
/.github         — GitHub Actions workflow
```

## Золотой стандарт 2026

Спецификация в `app/spec/golden_standard.yaml`:
- 8s grid: hook (0-0.7s) → A (0.7-3.5s) → B (3.5-7.0s) → laugh (7.0-8.0s)
- Killer word на ~6.85s
- Рот второго закрыт, пока говорит первый
- Артефакты: micro-jitter, exposure breathing, rolling shutter
- Safety: banned words auto-replace, device invisible

## Ограничения

- **Финальная правка текста под 8 секунд — на пользователе.** Timing Coach помогает оценить, но точность зависит от конкретного голоса.
- Demo mode использует шаблонные диалоги. Для уникальной генерации подключите LLM через API mode.
- Video understanding (фаза 2) — пока только мета-данные, без анализа содержимого видео.

## Безопасность

- Ремикс структуры и темпа, **без копирования личности или голоса** конкретных людей
- Все персонажи — вымышленные архетипы
- Banned words автоматически заменяются на safe-аналоги

## Лицензия

MIT
